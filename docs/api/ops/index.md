# Operations

A comprehensive library of tensor operations.

## Creation

Tensor factory functions: `zeros`, `ones`, `arange`, `linspace`, `from_numpy`, and more.

```{toctree}
:maxdepth: 1
:hidden:

creation/index
```

## Binary

Element-wise binary ops: `add`, `sub`, `mul`, `div`, `matmul`, `pow`, `outer`.

```{toctree}
:maxdepth: 1
:hidden:

binary/index
```

## Unary

Element-wise unary ops: `exp`, `log`, `sqrt`, `abs`, `sin`, `cos`, `relu`, and more.

```{toctree}
:maxdepth: 1
:hidden:

unary/index
```

## Reduction

Reduction and aggregation ops: `sum`, `mean`, `max`, `min`, `norm`, `var`, `std`.

```{toctree}
:maxdepth: 1
:hidden:

reduction/index
```

## Comparison

Element-wise comparison ops: `equal`, `not_equal`, `less`, `greater`, `where`.

```{toctree}
:maxdepth: 1
:hidden:

comparison/index
```

## Control Flow

Conditional execution ops: `cond`, `while_loop`, and other functional control flow.

```{toctree}
:maxdepth: 1
:hidden:

control_flow/index
```

## Multi-Output

Ops returning multiple tensors: `split`, `broadcast_tensors`, `unstack`.

```{toctree}
:maxdepth: 1
:hidden:

multi_output/index
```

## View & Reshape

Shape manipulation: `reshape`, `transpose`, `squeeze`, `unsqueeze`, `slice`, `gather`.

```{toctree}
:maxdepth: 1
:hidden:

view/index
```

## Communication

Cross-device communication primitives for distributed computation.

```{toctree}
:maxdepth: 1
:hidden:

communication/index
```
